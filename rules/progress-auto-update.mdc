---
description: 每次变更写 DETAIL.md；满足条件时更新 PROGRESS.md
alwaysApply: false
---

# PROGRESS.md / DETAIL.md 自动维护

> **权威规则**：本文件为唯一维护规范；`AGENTS.md` 仅作摘要。  
> **源文件位置**：`ai-kit/rules/`（`alwaysApply: false`，避免套件目录误加载）。  
> **部署**：复制到 **项目根** `.cursor/rules/` 后，将 frontmatter 改为 `alwaysApply: true`。

## 会话开始

- **只读** `PROGRESS.md` §1、§2（勿通读 `DETAIL.md`）。
- 查某次具体改动：在 `DETAIL.md` 内按日期/关键词检索，或结合 `git log`。
- **目录与包结构**以 `ARCHITECTURE.md` 为准；**能力现状**以 `PROGRESS.md` §1 为准。
- **大变更判定**：以 `AGENTS.md` 中「大变更判定」补充为准；无补充时适用下文默认。

## 每次实质文件变更后（必须写 DETAIL）

范围：业务源码、配置（`*.yml`/`*.xml`/`*.properties`/`*.env.example` 等）、项目文档、`.cursor/rules/*.mdc`（具体扩展名以 `ARCHITECTURE.md` 为准）。

在 `DETAIL.md` **末尾**追加一条，**禁止**修改或删除已有条目。

### DETAIL 条目模板（统一格式）

- 标题时刻：**北京时间（UTC+8，`Asia/Shanghai`）**；与 commit 对应时用 `git log --format=%ci`（应含 `+0800`），勿用 UTC 或整点估算冒充实录。

```markdown
## YYYY-MM-DD HH:mm:ss

- **commit**：`<hash>` 或 `—`（未提交）
- **路径**：`path/a`、`path/b`（关键文件，可省略次要）
- **说明**：做了什么、影响范围（可多行）
```

**不记录**：仅格式化、无实质变更；`.idea/`、`.ai/`、`node_modules/`、`target/`、`dist/` 等本地或构建产物目录。

纠错：不修改旧条目，在末尾追加新条，正文以「更正：…」开头并引用原条目时间。

## 何时更新 PROGRESS.md

### §3 里程碑（每轮最多一行）

**满足以下任一**则在本节表格**首行**追加**一行**摘要（勿与 commit 重复写两行）：

1. 本轮已执行 **`git commit`** → 一行对应本次 commit 要点（可附 hash 缩写）。
2. **大变更**（未 commit 也可，会话结束前判定一次）——优先看 `AGENTS.md`；未约定时参考：
   - 新增或删除 **≥1 套** 同等粒度的持久化/领域模块（如 Entity+Mapper、页面域+API 模块、等效一层）；
   - 单次会话改动 **≥3 个** 业务源码文件（不含纯测试/纯格式化）；
   - 修改 `ARCHITECTURE.md`、应用级数据源/中间件配置、或 **业务代码根** 下包/目录结构；
   - 某特性**首次可运行/可交付**（端到端能力、可演示流程）。
3. 会话结束且本轮有实质交付，且命中上表第 2 类 → 补写 §3（**不必等 commit**）。

**不算大变更**（只写 DETAIL）：单文件小修、重命名、局部重构、同一特性内分步微调、仅改注释/日志文案。

**禁止**：同一 commit 或同一交付在 §3 写两行；把每次小改写入 §3。

### §1 交付快照（与能力一致）

凡改变 §1 已描述的能力（增删模块/表/包、数量、「已齐/占位/未实现」状态），**必须**同步改 §1，**不等待** commit 或大变更判定。

只改实现细节、不改变 §1 语义时，只写 DETAIL，不改 §1。

### §2 已知缺口

已实现 → 删除对应行；新暴露且已在代码/文档中引用的未完成项 → 追加一行。

### 文首

有 §1/§2/§3 更新时，改 `> **最后更新**：YYYY-MM-DD`。

## 其他

- 架构/目录变更：同步 `ARCHITECTURE.md`（见 `AGENTS.md`）。
- `PROGRESS.md` §1 保持简短，不得展开全表实体/文件清单（细节查 `ARCHITECTURE.md` 或源码树）。

## 禁止

- 待办/计划不得写入 `PROGRESS.md` / `DETAIL.md`。
- 不得改删 `DETAIL.md` 历史。
- 不得跳过 `DETAIL.md` 结束会话。
- 不得通读 `DETAIL.md` 作为会话默认步骤。

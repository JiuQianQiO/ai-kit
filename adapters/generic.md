# 其它 AI 工具（generic）

未列入 `cursor` / `claude-code` / `trae` 时：

## 生成

- **仅**通用五件套：`AGENTS.md`、`PROGRESS.md`、`DETAIL.md`、`ARCHITECTURE.md`、`PROJECT_BRIEF.md`  
- 在 `AGENTS.md` 文首增加一行：**主 AI 工具**：{用户填写的名称}

## 建议用户

- 若工具支持项目级 Markdown 指令，将 `AGENTS.md` 全文或链接加入该工具的「项目规则」配置。  
- 若后续采用 Cursor / Claude Code / Trae，可再次运行 ai-kit 访谈并勾选对应工具。
